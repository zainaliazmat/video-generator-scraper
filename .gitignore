# Python
venv/
__pycache__/
*.pyc
*.pyo

# Data / outputs (regenerated by the scraper)
youtube_results.tsv
youtube_results_analysis.xlsx
youtube_content_ideas.md
history/
library.db
*.zip

.pytest_cache/

# Secrets — NEVER commit the API key
.env

# TTS audio outputs (regenerable)
assets/tts-samples/*.mp3

# OS / editor
.DS_Store
*.swp

# Obsidian per-machine UI state (the vault's .md files ARE tracked)
vault/.obsidian/

.gstack/
research/
studio

# Delivered-video heavy assets (regenerable; post-delivery cleanup rule in vault/CLAUDE.md)
Pompie Assets/

# derived per-agent views of tools/format.json — regenerated by `pipeline_check doctor`
tools/format/
