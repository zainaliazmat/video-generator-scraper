# 00 — Discovery (read-only)

Facts only. No evaluation, no recommendations. Every row cites a path.
Measured 2026-08-09 against the working tree at `7d3a7ad`.

Byte counts are exact (`wc -c`). Token figures are `bytes ÷ 4` and are marked
`≈`; Devanagari content tokenizes worse than 4 bytes/token, so script/storyboard
token estimates are floors, not ceilings.

---

## 1 · Repository structure

| Path | Kind | Size / count | Note |
|---|---|---|---|
| `CLAUDE.md` | project instructions | 1,591 B | routes channel work to `vault/CLAUDE.md` |
| `vault/CLAUDE.md` | vault operating manual | 6,970 B | read by 11 of 13 agents |
| `.claude/commands/finance-video.md` | the orchestrator | 24,902 B / 447 lines | the only command |
| `.claude/agents/fin-*.md` | 13 agents | 91,023 B total | table §2 |
| `.claude/skills/` | 2 skills | `lottie-master`, `vidiq` | no finance skill |
| `.claude/settings.local.json` | settings | 199 B | vidIQ key + `enabledMcpjsonServers` only; **no hooks, no permissions block** |
| `.mcp.json` | MCP config | 199 B | **one** server: `vidiq` (http) |
| `tools/pipeline_check.py` | stage gate | 92,905 B | 4 subcommands, 10 stage checks |
| `tools/format.json` | constants | 25,219 B / 24 top-level keys | source of truth |
| `tools/format/*.json` | derived per-agent views | 11 files, 102,312 B | gitignored (`.gitignore:38`), regenerated by `pipeline_check doctor` |
| `tools/*.py` | mechanics | 13 scripts, 227,218 B | `chapter_project.py` 22 KB, `cut_assemble.py` 22 KB |
| `tools/scaffold/` | project template | `package.json`, `blockframe.css`, `chapter-design.css`, fonts, `gsap.min.js`, `lottie.min.js`, `motion.js` | not a HyperFrames registry item |
| `vault/` | knowledge base | 450 `.md`, 6,643,490 B | §5 |
| `studio/` | HyperFrames monorepo **+** work-in-progress video projects | gitignored (`.gitignore:32`) | §3 |
| `package.json` (repo root) | — | **absent** | the repo root is not a Node project |
| hooks | — | **none in this repo** | `~/.claude/settings.json` has 3 global gstack hooks + ponytail statusline; none touch this pipeline |

Root also holds `AUDIT.md` (17.8 KB), `MAP.md` (13.1 KB), `NICHE_RESEARCH_2026-07.md`
(20.6 KB), `CHANGELOG-vault.md` (32.6 KB), `README.md`, `README.txt`, two stray
`files (N).zip`, and the `backend/` ytauto app — none referenced by any `fin-*` agent.

---

## 2 · Agent roster (13)

All 13 share `description: "Finance-pipeline stage. Invoked only by /finance-video.
Do not select for other work."` and are invoked by the orchestrator via the Task
tool with `slug, cut, tier, attempt` only
(`.claude/commands/finance-video.md:174-177`).

**Model tier:** only two agents declare one. `fin-archive.md:4` and
`fin-render.md:5` are `model: haiku`. The other 11 have no `model:` key and
inherit the session model (Opus 5).

**Context budget** = agent definition + every file the definition tells it to read
(`tools/format/<agent>.json`, `vault/CLAUDE.md`, named knowledge notes). It excludes
run artifacts (`script-<cut>.md`, `storyboard-<cut>.md`, `timing.json`), which are
larger still — see §4.

| # | Agent | Def (B) | Declared reads (B) | Load (B) | ≈tok | Tools granted | Writes | Upstream → Downstream |
|---|---|---|---|---|---|---|---|---|
| 1 | `fin-research` | 2,952 | 16,790 | **19,742** | ≈4.9k | Bash, Read, Grep, Glob, Write | `vault/knowledge/video-studies/<slug>.md` | (intake) → `fin-facts` |
| 2 | `fin-facts` | 2,925 | 34,941 | **37,866** | ≈9.5k | WebSearch, WebFetch, Read, Write, Grep | `vault/videos/<slug>/facts-staging.md` | `fin-research` → `fin-script` |
| 3 | `fin-script` | 4,169 | 73,846 | **78,015** | ≈19.5k | Read, Write, Grep, Glob | `script-<cut>.md` | `fin-facts` → `fin-audit` |
| 4 | `fin-audit` | 3,821 | 15,984 | **19,805** | ≈5.0k | Read, Write, Edit, Grep, WebFetch, WebSearch | `audit-<cut>.md` | `fin-script` → `fin-voice` |
| 5 | `fin-voice` | 2,765 | 11,936 | **14,701** | ≈3.7k | Bash, Read, Write | `assets/voice/lines.json`, `timing.json`, `gen_vo_<cut>.sh` | `fin-audit` → `fin-storyboard` |
| 6 | `fin-storyboard` | 8,968 | 89,408 | **98,376** | ≈24.6k | Read, Write, Grep | `storyboard-<cut>.md` | `fin-voice` → `fin-assets` |
| 7 | `fin-assets` | 16,582 | 52,892 | **69,474** | ≈17.4k | Bash, Read, Write, Edit | `assets-ch<N>/final/*.jpg`, `assets/lottie/` | `fin-storyboard` → `fin-build` |
| 8 | `fin-build` | 15,452 | 99,270 | **114,722** | ≈28.7k | Read, Write, Edit, Bash, Glob | `<slug>-<cut>-ch<N>/index.html`, `build.mjs`, `assets/audio.json` | `fin-assets` → `fin-render` |
| 9 | `fin-render` | 4,840 | 11,500 | **16,340** | ≈4.1k | Bash, Read, Glob | `renders/DRAFT-ch<N>.mp4`, `SHEET-ch<N>.jpg` | `fin-build` → `fin-editor` |
| 10 | `fin-editor` | 8,894 | 49,672 | **58,566** | ≈14.6k | Bash, Read, Grep, Glob | `logs/editor-<cut>-ch<N>-<n>.md` | `fin-render` → `fin-ceo` |
| 11 | `fin-ceo` | 5,708 | 31,320 | **37,028** | ≈9.3k | Bash, Read, Grep, Glob | `logs/ceo-<cut>-ch<N>-<n>.md` | `fin-editor` → lock |
| 12 | `fin-package` | 11,356 | 35,701 | **47,057** | ≈11.8k | Bash, Read, Write, Grep, WebSearch | `youtube-metadata-<cut>.md`, `<slug>-thumbs/` | (post-render) → close-out |
| 13 | `fin-archive` | 2,591 | 78,586 | **81,177** | ≈20.3k | Read, Write, Edit, Glob | `vault/videos/<slug>/index.md`, `src/` | close-out → (end) |
| | **TOTAL** | **91,023** | **601,846** | **692,869** | **≈173k** | | | |

Purpose, in each agent's own words (first descriptive sentence):

| Agent | Self-described purpose |
|---|---|
| `fin-research` | "the competitor-study stage of the finance-video pipeline" (`fin-research.md:7`) |
| `fin-facts` | "the money-number sourcing stage of the finance-video pipeline" (`fin-facts.md:7`) |
| `fin-script` | "the script-writing stage… Runs once per cut" (`fin-script.md:7`) |
| `fin-audit` | "the adversarial audit stage — **gate one**, the last check before real money is spent on TTS… Your job is to BREAK the script" (`fin-audit.md:7-9`) |
| `fin-voice` | "the TTS + timing stage… the mechanics live in a script; you only prepare its input and read its output" (`fin-voice.md:7-9`) |
| `fin-storyboard` | "the storyboard stage. Runs once per cut" (`fin-storyboard.md:7`) |
| `fin-assets` | "the stock-image stage… your job is to LOOK at every image, not to fetch" (`fin-assets.md:7-9`) |
| `fin-build` | "the composition-build stage. Runs once per cut" (`fin-build.md:7`) |
| `fin-render` | "the render + QA stage — **gate two**, the last check before an ~18-minute encode" (`fin-render.md:8-9`) |
| `fin-editor` | "the **editor**… You do not edit anything yourself — you are the eyes, `fin-build` is the hands" (`fin-editor.md:7-9`) |
| `fin-ceo` | "the **CEO**… Would a viewer keep watching, and would I put my channel's name on this?" (`fin-ceo.md:7-12`) |
| `fin-package` | "the thumbnail + publish-pack + compliance stage. Runs once per cut" (`fin-package.md:7`) |
| `fin-archive` | "the vault close-out stage. Runs once per run (after both cuts)" (`fin-archive.md:7`) |

### 2.1 · Stage-check coverage

`tools/pipeline_check.py --help` accepts exactly these stage names:

```
assets · audit · build · facts · package · render · research · script · storyboard · voice
```

Subcommands: `check`, `mark`, `doctor`, `architecture`.

`.claude/commands/finance-video.md:13-14` states "A stage is `done` only when
`tools/pipeline_check.py` says so." Three agents have no stage in that list:
**`fin-editor`, `fin-ceo`, `fin-archive`**. The orchestrator's transition counter
prints `<n>/18` (`finance-video.md:173`) — 18 stage-slots across 13 agent types.

---

## 3 · HyperFrames — the live surface

### 3.1 · Three different HyperFrames versions are present at once

| Where | Version | Evidence |
|---|---|---|
| `studio/` monorepo checkout (`packages/cli`) | **0.7.10** | `studio/packages/cli/package.json:3`; `node studio/packages/cli/dist/cli.js --help` prints `v0.7.10` |
| Pinned per video project | **0.7.66** | `studio/videos/passive-income-number-hi/package.json` `devDependencies.hyperframes: "0.7.66"`; `npx hyperframes --help` in that dir prints `v0.7.66` |
| npm `latest` | **0.7.102** | `npx hyperframes@latest --version`; also `~/.hyperframes/config.json` `"latestVersion": "0.7.102"` |

`studio/` is **not its own git repository** (`ls studio/.git` → does not exist) and is
gitignored by the parent (`.gitignore:32`, confirmed by `git check-ignore -v studio`).
`CLAUDE.md:14-15` states studio "is its own repo" — that is not true on this machine.
`studio/package.json` is `hyperframes-monorepo` from `github.com/heygen-com/hyperframes`.

### 3.2 · CLI commands — what actually exists

`hyperframes --help` at the **pinned 0.7.66** (the version the pipeline runs):

```
init  add  capture  catalog  preview  present  publish  render
lint  check  validate  beats  inspect  keyframes  snapshot  grade-compare  compare  info  compositions  docs
benchmark  browser  doctor  upgrade
cloud  lambda  cloudrun
skills  transcribe  tts  remove-background
auth  feedback  telemetry
```

The **0.7.10** build in `studio/packages/cli` is missing `check`, `validate`,
`keyframes`, `grade-compare`, and `compare`. Verified two ways: they are absent from
its `--help`, and `ls studio/packages/cli/src/commands/` has no `check.ts` /
`keyframes.ts` / `compare.ts` (it has `validate.ts`, `layout.ts`, `inspect.ts`).

⚠️ Probe caveat: `hyperframes <unknown-command> --help` prints the general help and
**exits 0**, so `--help` exit status cannot be used to test for a subcommand. All
existence claims above are from help-listing text plus source files.

### 3.3 · Prompt assumptions vs. reality

| Prompt author's belief | Reality | Evidence |
|---|---|---|
| "a CLI with `init`, `lint`, `check`, `snapshot`, `preview`, `render`, `publish`, `doctor`, plus `cloud render` and Lambda" | **Confirmed** at 0.7.66. `cloud`, `lambda`, and additionally `cloudrun` (GCP) exist. | `npx hyperframes --help` in `studio/videos/passive-income-number-hi/` |
| "a catalog of 50+ prebuilt blocks/templates" | **147 items on disk / 142 in the manifest**: 109 `hyperframes:block`, 25 `hyperframes:component`, 8 `hyperframes:example` in `registry.json` (13 example dirs on disk) | `studio/registry/registry.json`; `ls studio/registry/{blocks,components,examples}` |
| "~19 installable agent skills" | **19 skills exist in the monorepo** (`studio/skills/`), but **only 7 are installed** into `~/.claude/skills/`: `hyperframes`, `-animation`, `-cli`, `-core`, `-creative`, `-keyframes`, `-registry`. Not installed: `embedded-captions`, `faceless-explainer`, `general-video`, `hyperframes-media`, `media-use`, `motion-graphics`, `music-to-video`, `product-launch-video`, `pr-to-video`, `remotion-to-hyperframes`, `slideshow`, `talking-head-recut`, `website-to-video`. `~/.hyperframes/config.json` reports `"skillsUpdateAvailable": true, "skillsOutdatedCount": 5`. | `ls studio/skills/`, `ls ~/.claude/skills/`, `studio/skills-lock.json` |
| "HTML *is* the timeline; deterministic frame-exact rendering via headless Chrome + FFmpeg" | **Confirmed** | `~/.claude/skills/hyperframes-core/SKILL.md:8` |
| "Rule of Three contract (root `data-composition-id`/`data-width`/`data-height`; timed elements `class="clip"`/`data-start`/`data-duration`/`data-track-index`; GSAP timelines seekable)" | **Partially confirmed, and the phrase "Rule of Three" does not appear.** The installed core skill documents `class="clip"`, `data-start`, `data-duration`, `data-track-index`, `data-composition-id`, and "exactly one `gsap.timeline({ paused: true })` at `window.__timelines["<id>"]`, built synchronously at page load; render duration = root `data-duration`". `data-width`/`data-height` are not in `SKILL.md` — the full attribute list is deferred to `references/data-attributes.md`. | `~/.claude/skills/hyperframes-core/SKILL.md:18,22,40,44,54,71,73` |
| "a Frame Adapter pattern" | **Not found under that name.** The installed skills document **seven runtime adapters** (GSAP default, Lottie, Three.js, Anime.js, CSS keyframes, WAAPI, TypeGPU) at `hyperframes-animation/adapters/`. | `~/.claude/skills/hyperframes-animation/SKILL.md` frontmatter |

### 3.4 · MCP surface

| Server | Configured in | Status |
|---|---|---|
| `vidiq` | `.mcp.json` (project), enabled in `.claude/settings.local.json` | available; 46 `mcp__vidiq__*` tools |
| **HyperFrames by HeyGen** | claude.ai connector (not this repo) | **unauthenticated — its tools cannot be called in this session, so no input schemas are obtainable.** Same for claude.ai Google Drive. |

**Consequence for the audit:** the exact input schemas of HyperFrames MCP tools —
a Phase 0 deliverable — **cannot be produced**. Every HyperFrames capability recorded
above is from the CLI and the installed skills, not from MCP. See §6, question 1.

### 3.5 · How much of HyperFrames the pipeline actually uses

| HyperFrames surface | Used by the pipeline? | Evidence |
|---|---|---|
| `hyperframes render` | yes | `fin-render.md:34`, `tools/cut_assemble.py:330` |
| `hyperframes snapshot` | yes | `fin-render.md:23`, `fin-build.md:25,240` |
| `hyperframes check` | yes (via `npm run check`) | `finance-video.md:257`, `fin-build.md:178,195`; project `package.json` maps `"check": "hyperframes check"` |
| `hyperframes lint` | indirectly (inside `check`) | `tools/format.json:249` |
| `hyperframes init` | **no** — projects are scaffolded by `tools/chapter_project.py` + `tools/scaffold/` | `tools/chapter_project.py:1-30` |
| `hyperframes add` / `catalog` / registry blocks | **no** | no `hyperframes.json` under `studio/videos/*` (only in already-archived `vault/videos/*/src/`); no agent references `hyperframes add` |
| `cloud` / `lambda` / `cloudrun` | **no** | `finance-video.md:383-385` names cloud/Lambda as a not-taken option |
| `preview`, `present`, `publish`, `beats`, `compare`, `keyframes`, `grade-compare`, `transcribe`, `tts` | **no** | not referenced in `.claude/` or `tools/` |
| installed HyperFrames **skills** | **no** — no `fin-*` agent references any `hyperframes-*` skill | grep over `.claude/agents/` |

Compositions are emitted by a **per-project hand-written `build.mjs`**, one per chapter:

| File | Bytes |
|---|---|
| `studio/videos/passive-income-number-hi-ch1/build.mjs` | 51,985 |
| `…-hi-ch2/build.mjs` | 71,271 |
| `…-hi-ch3/build.mjs` | 68,929 |
| `…-en-ch1/build.mjs` | 39,508 |
| `…-en-ch2/build.mjs` | 59,494 |
| `…-en-ch3/build.mjs` | 66,645 |
| **total, one video** | **357,832** |

Generated output, e.g. `studio/videos/passive-income-number-hi-ch2/index.html` = 50,576 B.

---

## 4 · Run trace — `passive-income-number` (most recent, still open)

State file: `vault/videos/passive-income-number/run.json` (11,775 B, last written
2026-08-09 07:53). Tier `medium`, target 510 s, architecture `blockframe-9`, started
2026-08-06.

### 4.1 · Completion state

| Cut | Chapters planned | `locked` | Other states |
|---|---|---|---|
| hi | 7 | 2 (ch1, ch2) | ch3 `s22_replaced_REBUILD_PENDING`, ch4 `assets_done_build_pending`, ch5–7 `pending` |
| en | 6 | 2 (ch1, ch2) | ch3 `redrafted_awaiting_editor_r2`, ch4 `assets_done_build_pending`, ch5–6 `pending` |

`run.json.stages` records 14 of 18 stage-slots; `fin-package` and `fin-archive` were
never invoked (no `logs/fin-package*`, no `logs/fin-archive*`). The run has spent
**300 of 350 permitted ElevenLabs calls** (`run.json` `budget`) and **40 of 53 vidIQ
credits** (`run.json` `vidiq_spend`) while producing **4 locked chapters out of 13**.

### 4.2 · Where the invocations went

98 log files, 1,330,173 B total.

| Stage family | Invocations | Log bytes | ≈tok (logs only) |
|---|---|---|---|
| `fin-assets` | 23 | 335,742 | ≈84k |
| `fin-build` | 19 | 284,708 | ≈71k |
| `fin-editor` | 16 | 210,990 | ≈53k |
| `fin-render` | 12 | 214,536 | ≈54k |
| `fin-ceo` | 6 | 74,303 | ≈19k |
| `fin-script` | 5 | 57,691 | ≈14k |
| `fin-audit` | 5 | 26,154 | ≈7k |
| `fin-voice` | 5 | 61,638 | ≈15k |
| `fin-storyboard` | 4 | 38,196 | ≈10k |
| `fin-research` | 2 | 16,530 | ≈4k |
| `fin-facts` | 1 | 9,685 | ≈2k |
| `fin-package` | 0 | 0 | — |
| `fin-archive` | 0 | 0 | — |
| **total** | **98** | **1,330,173** | **≈333k** |

Log bytes are the agents' *output* only. Each invocation additionally paid its
context load from §2 plus the run artifacts it reads:

| Run artifact | Bytes | Read by |
|---|---|---|
| `notes.md` | 157,996 | orchestrator (`finance-video.md:370-373` says quote ONE ruling, not the file) |
| `storyboard-hi.md` | 100,294 | `fin-assets`, `fin-build`, `fin-editor`, `fin-ceo` |
| `script-hi.md` | 99,696 | `fin-audit`, `fin-voice`, `fin-editor` |
| `storyboard-en.md` | 84,435 | as above (en) |
| `script-en.md` | 64,121 | as above (en) |
| `audit-hi.md` | 33,248 | `fin-voice` |
| `audit-en.md` | 32,627 | `fin-voice` |
| `facts-staging.md` | 31,927 | `fin-script`, `fin-editor` |

### 4.3 · Retries across all six recorded runs

Counted as `sum(stage.attempt) − len(stages)` from each `run.json`:

| Run | Stages recorded | Total attempts | Retries |
|---|---|---|---|
| `pay-yourself-first` | 18 | 18 | **0** |
| `credit-history` | 18 | 23 | 5 |
| `good-debt-vs-bad-debt` | 18 | 25 | 7 |
| `first-lakh-first-thousand` | 18 | 27 | 9 |
| `japanese-money-methods` | 15 | 35 | **20** |
| `passive-income-number` | 14 | 31 | **17** (incomplete) |

The two most recent runs are the two most retry-heavy, and both are the two that
adopted the chapter loop (`finance-video.md:239`, dated 2026-08-04).

### 4.4 · Human-rescue points on record

The run's narrative file `vault/videos/passive-income-number/notes.md` is 157,996 B
with **322 headings**. Heading families, verbatim:

- `owed.*` — 13 open obligations (e.g. `owed.cascade_offsets_ignore_the_voice`,
  `owed.fade_clobbers_authored_opacity`, `owed.storyboard_hi_ladder_stale_in_three_places`)
- `tool_fixes_this_session.*` — 6 code defects found and fixed mid-run
  (`cues_py_popeach_off_by_one`, `cue_min_gap_never_enforced`,
  `comma_fix_protects_the_wrong_class`, `stale_log_guard`, two CSS)
- `incidents.*` — 3 (`api_outage_2026-08-08`, `api_outage_2_2026-08-08`,
  `session_limit_2026-08-08_1849`)
- `rulings_binding_on_both_cuts.*` — 4 creator rulings issued mid-run
- `hook_gate_hi.*` / `hook_gate_en.*` — 17 headings of measured/modelled gate values
- `hi_length_decision.*` — 6 headings recording a creator-forced script re-length

`run.json.constraints.derived_income_carries_assumption` records a rule "EXTENDED
2026-08-07 during ch2 review" — i.e. a correctness constraint discovered by human
review after the build gate had already passed the scene "correctly, by the letter."

`run.json.style_decision` records that chapters 1–2 of both cuts were re-scripted to
"style E" and that "chapters 3+ must be restyled to match" — a mid-run rewrite of
already-locked upstream output.

There is **no `runs/` directory, no `metrics.json`, no `decisions.jsonl`, no
`interventions.md`, and no token accounting anywhere in the repo.** Token cost per
run cannot be recovered from artifacts; §4.2 is a proxy (output bytes), not a
measurement.

---

## 5 · Vault survey

`vault/` — 450 `.md` files, 6,643,490 B total, max folder depth 5.

### 5.1 · Composition

| Folder | Notes | Note |
|---|---|---|
| `vault/videos/*/logs/` | **249** | per-attempt agent logs |
| `vault/knowledge/` | 24 (+12 in `finance-audit-2026-07-29/_archive`, +6 `niches`, +5 `video-studies`) | the actual knowledge |
| `vault/videos/*/` (non-log) | ~90 | milestone notes, scripts, storyboards, `src/` |
| `vault/workflows/` | 5 | 84,857 B |
| `vault/skills/` | 3 | 79,554 B |
| `vault/templates/` | 4 | 10,522 B |
| `vault/index.md` | 1 | **39,798 B** |
| `vault/CLAUDE.md` | 1 | 6,970 B |

Ten largest knowledge notes:

| Bytes | Note |
|---|---|
| 35,087 | `knowledge/design-finance-blockframe.md` |
| 24,380 | `knowledge/design-thumbnail-ai-enhance.md` |
| 23,912 | `knowledge/claude-design-mcp.md` |
| 22,583 | `knowledge/design-chapter-archetypes.md` |
| 20,122 | `knowledge/stock-photo-sourcing.md` |
| 19,972 | `knowledge/money-facts-2026.md` |
| 14,831 | `knowledge/design-icons-emoji-lottie.md` |
| 12,523 | `knowledge/evidence-discipline.md` |
| 11,846 | `knowledge/best-practices.md` |
| 9,550 | `knowledge/design-vector-only-chapter.md` |

Largest single notes overall: `skills/long_form_scripting.md` 30,768 · `workflows/runbook-history-slideshow.md` 27,479 · `skills/hyperframes_production.md` 27,092 · `skills/youtube_channel_skill.md` 22,083.

### 5.2 · Frontmatter

**310 / 450 (68.9%)** of notes open with `---`. Keys in use, with occurrence counts:

| Key | Count | Key | Count |
|---|---|---|---|
| `updated` | 309 | `cut` | 6 |
| `summary` | 301 | `tier` | 4 |
| `source` | 288 | `one-write-rule` | 2 |
| `stage` | 164 | `invocation` | 2 |
| `status` | 15 | `verified_by` | 1 |
| `slug` | 9 | `url` | 1 |
| `attempt` | 9 | `date` | 1 |
| `revision` | 7 | | |
| `scope` | 6 | | |

`vault/CLAUDE.md:112-115` mandates exactly three: `summary`, `updated`, `source`.
There is **no `id`, no `type`, no closed-set classification, no `confidence`, no
`verified_on`, no `supersedes`** anywhere in the vault. `status` appears on 15 notes.

### 5.3 · Link graph

- 980 wikilink instances, 322 unique targets
- **29 broken links.** Two distinct causes:
  - relative-path links written as wikilinks: `[[../../CLAUDE.md]]`,
    `[[../../../CLAUDE.md]]`, `[[../../videos/japanese-money-methods/run]]`,
    `[[../../..]]` (`knowledge/video-studies/*.md`, `knowledge/niches/us-market-2026.md`)
  - links to **auto-memory slugs that live outside the vault**, in
    `~/.claude/projects/.../memory/`: `[[scene-image-prompt-rules]]`,
    `[[depiction-no-prophet-faces]]`, `[[finance-every-frame-has-image]]`,
    `[[english-titles-descriptions]]`, `[[two-home-memory-architecture]]`
- **249 orphans** (never the target of any wikilink) — 228 of them are
  `vault/videos/*/logs/*.md`, i.e. the entire agent-log corpus is unreachable from
  the index. The remaining 21 are archived `src/` notes and milestone notes.

### 5.4 · Which notes agents read

From the `.claude/agents/*.md` path references (§2). Read-frequency across the roster:

| Note | Agents that read it |
|---|---|
| `vault/CLAUDE.md` | 11 (all but `fin-editor`, `fin-ceo`) |
| `knowledge/design-chapter-archetypes.md` | 4 (`storyboard`, `build`, `editor`, `ceo`) |
| `knowledge/design-finance-blockframe.md` | 2 (`storyboard`, `build`) |
| `knowledge/design-icons-emoji-lottie.md` | 2 (`assets`, `build`) |
| `knowledge/money-facts-2026.md` | 2 (`facts`, `archive`) |
| `knowledge/video-studies/<slug>.md` | 3 (`research` writes; `script`, `ceo` read) |
| `knowledge/stock-photo-sourcing.md` | 1 (`assets`) |
| `knowledge/evidence-discipline.md` | 1 (`editor`) |
| `knowledge/channels.md` | 1 (`ceo`) |
| `knowledge/us-english-script-style.md`, `knowledge/niches/india-finance-market.md` | 1 (`script`) |
| `knowledge/best-practices.md`, `vault/index.md` | 1 (`archive`) |
| `knowledge/subscription-economics-2026.md` | 1 (`facts`) |
| `skills/long_form_scripting.md`, `workflows/voiceover-tts.md` | 1 (`script`) |
| `workflows/video-study.md`, `templates/video-study.md` | 1 (`research`) |
| `templates/storyboard-template-finance.md` | 1 (`storyboard`) |
| `knowledge/design-thumbnail-ai-enhance.md` | 1 (`package`) |

**Never read by any agent** (13 of 24 top-level knowledge notes):
`claude-design-mcp.md`, `design-vector-only-chapter.md`, `design-chapter-sound.md`,
`urdu-script-style.md`, `unshipped-experiments.md`, `chapter-pipeline-traps.md`,
`vidiq-mcp.md`, `design-cinematic-history.md`, `haryanvi-hindi-script-style.md`,
`design-techtooltester.md`, `indian-business-culture-slang.md`, `monetization.md`,
`scraping-playbook.md`. Also unread: all 5 `workflows/`, 2 of 3 `skills/`
(`hyperframes_production.md`, `youtube_channel_skill.md`), and `vault/index.md`
(read only by `fin-archive`, which writes to it).

⚠️ `knowledge/chapter-pipeline-traps.md` (6,489 B) is named for the exact failure
class §4.3 shows dominating the last two runs, and no agent reads it.

---

## 6 · Facts that contradict the brief

Recorded here without recommendation, per the phase contract.

1. **HyperFrames MCP is unauthenticated in this session.** Its tool input schemas — an
   explicit Phase 0 deliverable — cannot be obtained. Everything in §3 comes from the
   CLI and installed skills instead.
2. **Three HyperFrames versions coexist** (0.7.10 monorepo / 0.7.66 pinned / 0.7.102
   npm latest) and they do not have the same command set: `check`, the gate the
   orchestrator depends on at `finance-video.md:257`, does not exist in 0.7.10.
3. **The catalog is 147 items, not "50+"**; the skill count is **19 published but 7
   installed**, not 19 installable-and-installed; and `~/.hyperframes/config.json`
   reports 5 of those 7 are outdated.
4. **"Frame Adapter pattern" does not exist under that name.** The installed skills
   document seven *runtime adapters*. "Rule of Three" likewise appears nowhere.
5. **The pipeline uses ~4 of ~30 CLI commands and 0 of 147 registry items.** Every
   composition is emitted by a hand-written per-chapter `build.mjs` (357,832 B for
   one video), and project scaffolding is `tools/chapter_project.py`, not
   `hyperframes init`.
6. **`studio/` is not a git repository** despite `CLAUDE.md:14-15`, and is gitignored.
   357 KB of per-video `build.mjs` and every in-flight composition are therefore
   backed up by nothing.
7. **Three of the thirteen agents have no stage check** (`fin-editor`, `fin-ceo`,
   `fin-archive`), so the orchestrator's stated invariant "a stage is `done` only when
   `pipeline_check.py` says so" (`finance-video.md:13-14`) cannot hold for them.
8. ~~**The named pain point "runs cost too many tokens" cannot be measured from
   artifacts.**~~ **SUPERSEDED 2026-08-09 — see [05-baseline.md](05-baseline.md).** No file
   *in the repo* records tokens, and that part stands. But Claude Code writes per-subagent
   transcripts with full usage records to
   `~/.claude/projects/<slug>/<session>/subagents/agent-*.jsonl`, so a real baseline was
   recoverable without a new run: **70,466,242 billed tokens for 4 locked chapters**, 99.2%
   coverage. §4.2's output-byte proxy is superseded by that measurement.
9. **The last complete-ish run is 98 invocations for 4 locked chapters of 13**, with
   86% of ElevenLabs budget and 75% of vidIQ credits already spent.
10. **`fin-package` and `fin-archive` have never run on the current pipeline shape** —
    zero logs on the last run, and the chapter loop that reshaped everything upstream
    is dated 2026-08-04, after the last run that reached them.
11. **68.9% frontmatter coverage with 3 informal keys**, 29 broken links, 249 orphans
    (228 of them agent logs). 13 of 24 knowledge notes are read by no agent.

---

## 7 · Blocking questions (max 5)

1. **HyperFrames MCP.** It needs authorization via your claude.ai connector settings
   before I can read its tool schemas. Should Phase 1 proceed CLI-only and treat MCP
   as `UNVERIFIED`, or do you want to authorize first and have me re-run §3.4?
2. **Which HyperFrames version is canonical?** The pipeline pins 0.7.66; npm latest
   is 0.7.102; the `studio/` checkout is 0.7.10. Phase 1's
   "HyperFrames-native equivalent" column depends on which one I evaluate against.
3. **Is the `passive-income-number` run to be resumed, or is it the corpse I audit?**
   If it is being resumed, Phase 2's migration plan must not disturb it.
4. **Baseline measurement (Phase 5) requires one full run of the *current* system**
   with token accounting instrumented. That means real ElevenLabs + image spend. Do
   you want (a) a real baseline run, (b) a `FIN_FAKE_APIS=1` rehearsal baseline
   (`finance-video.md:29-30`) with zero spend and no image/voice quality signal, or
   (c) reconstruct the baseline from the six existing `run.json` + log corpus only?
5. **`studio/` is unbacked and ungitted.** Do you want that fixed before any
   refactor touches it?

---

*Phase 0 complete. No files outside `audit/` were created or modified.*
