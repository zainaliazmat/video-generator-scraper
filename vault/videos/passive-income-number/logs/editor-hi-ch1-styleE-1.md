# editor · passive-income-number · hi · chapter 1 · attempt 1 (STYLE E REBUILD)
VERDICT: REWORK

Reviewed: `renders/DRAFT-ch1.mp4` (08-08 10:43, 1275f / 42.500s / CFR 30) and
`renders/SHEET-ch1.jpg` (08-08 10:58, regenerated from that draft). `index.html`
is 10:39, so composition → draft → sheet are mutually consistent and the
missing `fin-build-hi-ch1-2.md` does not leave a gap I had to take on trust:
the fix it would have recorded (`#s4-stmt`) I re-verified myself, and so did
every other joint — see "What is working".

The style-A verdicts (`editor-hi-ch1-1.md`, `-2.md`) are NOT carried forward.
One of their rulings is explicitly re-opened below (#1) because the file was
kept while the LINE it sits under changed.

## Findings

| # | scene | severity | what | why it fails | fix |
|---|-------|----------|------|--------------|-----|
| 1 | s3 | **blocker** | The chai-glass window sill under `BEFORE NOON / "Money lands in the account."` (line 1.3, «आपके खाते में पैसे आ जाते हैं»). `.src` = `chai glass on a window sill morning sunlight indoors@pexels` | Cover the words and this frame says *"a quiet morning at home"*. Neither subject the line NAMES is present — no money, no account, no bank artefact, no phone, no screen. That is the sound-off rule's own failing condition, and it lands on the promise beat: the hook gate is measured on this clip. **This is not the ruling `editor-hi-ch1-2.md` #4 made.** That one cleared the same file against style A's *"Tea. The window. One buzz."*, a line the photograph genuinely said. Style E replaced the line; the acceptance does not travel with the file. **And the device that would have excused it is not in the build.** The script declares 1.3+1.4 as ONE continuous zoom — s3 the place and the hour, s4 the arrival — so s3 was never meant to stand alone. §10 overrode the zoom to a dissolve, which leaves s3 standing alone anyway, saying nothing about money for 3.9s | **One fetch, and it closes #2 as well. Re-fetch `s3` as a single domestic morning frame carrying a steel chai glass AND a whole, dark-screen phone on the same counter** — then derive `s4` as a tighter crop of that same file and restore the script's declared continuous zoom (`plateKen` chained across the joint, one file, one `background-position`), which is the shape `editor-en-ch1-2` #1 and `ceo-en-ch1-1` landed on for the sibling cut. ⚠ The shot class is buyable — the en cut bought it on `still life of a mobile phone and a cup of coffee on a dark table@pexels`. **Do not reuse that file:** I opened it, it is a white-terrazzo café table with a ceramic coffee mug and a eucalyptus sprig, i.e. the wrong market and the wrong drink for a ₹ cut. Query for the Indian equivalent (steel/glass chai + dark-screen phone, wooden counter or sill, warm low-key original, no face, no lit screen — §10's black-screen rule still holds) |
| 2 | s3 → s4 | should-fix | **The joint I was asked to rule on. It reads as TWO PICTURES, not one place.** The numbers were right and I am not contradicting them — no step, no pop, nothing to see in scdet or luminance. What the numbers cannot see is that the two frames share no object, no depth and no light direction: s3 is a mid-shot of a mullioned window sill with foliage bokeh behind it; s4 is a warm macro of a black phone slab that fills the frame edge to edge. The scale jump alone is mid-shot → extreme close-up | The override's stated justification is *"a separate fetch on the same counter material (steel + wood, same light) so the place still reads as one place"*. It does not: **wood is not an identifier in this chapter** — six of eight grounds are wood (#4), so a shared timber tone reads as the chapter's house style, not as this room. It does not read as a glitch or as a different house; it reads as a mood cut, which is why this is should-fix and not a blocker. It simply does not deliver what it was declared to deliver | **Cleared by #1** — one source file for both scenes makes the joint continuous by construction. If #1 is somehow not achievable, the fallback is to accept the dissolve and say so in the storyboard as a plain cut rather than as a same-place override, and to open s4 wider (see below) |
| 3 | s4 | should-fix | For its first ~1.5s (12.2–13.7s) s4's ken starts at scale 1.084 hard on the phone body, so the frame is a near-black rectangle with no readable subject — at 13.3s you cannot tell it is a phone. It only becomes legible once the pull-out reveals the table edge around 15s | This is the payoff frame of the cold open and it opens on nothing. It also makes the joint in #2 worse than it needs to be: the dissolve's destination is a black field, so there is no object for the eye to carry across | Start s4's ken wider (the phone whole in frame from +0.00) and let it settle, rather than pulling out from inside the phone body. If #1 lands, this is the crop decision on the new file — make the phone whole and dominant under the `.p-d` band (Lottie stage x657–1297, y602–902) |
| 4 | s7 | should-fix | «वो नंबर पाँच सीढ़ियों में खुलेगा» / `FIVE RUNGS` runs as a flat photograph of a stone staircase with type over it. No drawn layer (`art: off`) | **The line's point is a COUNT and the photograph cannot count.** `format.json vector_art.lottie.reach_for_it_when` names COUNT explicitly; the staircase says *"steps, start at the bottom"* and says nothing about *five*. The chapter is at **1 of 4** Lotties, so it is not a cap problem — and this is the chapter's one genuine unclaimed motion beat | Five drawn rung marks, the lowest one landing/lighting on the statement beat. **This is icon-first, not a Lottie fetch** (`vector_art.icon_first`): an inline `<svg class="icon">` + `draw()`, solid fills / ≥9px strokes per the chapter_design gotcha, assembled by about +3.3s so the contact sheet samples a finished row. It means dropping `centred` on s7 and letting arch-B's plate `[1120,150,860,610]` own the marks — which is that archetype's declared use, and the half is then no longer empty. ⚠ **This does not breach §3's no-counter rule.** §3 forbids showing the viewer the video is chapter-based ("RUNG 3 OF 5"); five rungs on the scene whose own VO says *five rungs* is the line being drawn, not a progress indicator. Do not ping-pong on this |
| 5 | s1 + ground | should-fix | **The ground-monotony question I was asked to rule on: 6 of 8, not the 5 of 8 fin-assets declared** — s1, s2, s3, s4, s6, s8 are all brown wood grain (s5 is a painted door + masonry, s7 is stone). Separately s1 and s4 are the chapter's one near-pair: both are a dark phone lying on dark wood, and on the sheet they read as the same statement twice | **Ruling: NOT a blocker as it stands.** The subjects are strongly different object to object (clock, glass, phone, plaque, jars, staircase, stamp) and the locked grade is dark enough that timber tone is not what the eye is reading, so the sheet does not read as sameness the way en ch1's 6/8 did. But two things make it worth one action: the s1/s4 pair is real, and **if #1 lands a counter frame for s3+s4, s1 becomes the third dark-wood-plus-phone frame in five scenes** | **Re-fetch `s1` onto TEXTILE** — bed linen or a bedside cloth, which is the script's own cue for 1.1 (*"a bedside table in a quiet Indian bedroom"*) and is not currently honoured by a dark wooden tabletop. Breaks the s1/s4 pair, takes wood to 5/8, and gives the video's very first frame a warmer, more readable opening than the near-black it has now. Do this **only after #1**, so both phone frames are decided together |
| 6 | s4 | note | **The Lottie question I was asked to rule on: the LOTTIE IS RIGHT, THE COMMENT IS WRONG.** Verified from the generator and from the encode, not from the artefact | `assets/lottie/src/phone-notify-credit.py` draws exactly one ₹, as five round-capped strokes on the 76×76 app-mark tile (`rupee()`, lines 70–108), and deliberately leaves the amount as bars: *"the digits stay bars; only the currency is named"*, and a second glyph at the head of the amount bar *"was tried and reads as a repeat at card size, not as emphasis"*. Confirmed in the encode at 16.9s — the ₹ is unambiguous at 1080p, so the round-1 blocker (*a banner with no currency mark says "a notification arrived"*) is genuinely closed. fin-render read the tile as a generic icon; it is the ₹ | `index.html`'s s4 scene comment says *"the card reads ₹ • • • •"*. It does not — it reads ₹-tile + masked bars. **Comment edit only, no re-render**: change it to "the ₹ is the app mark; the amount is a masked bar". Record the same in the storyboard so ch7's callback does not go looking for dots |
| 7 | s8 | note | **The 1280px / ~1.63× question I was asked to rule on: ACCEPTABLE, do not re-fetch.** | 1:1 crops from the encode at 37.8s and 39.5s show real micro-texture in the stamp handle's wood and a crisp handle edge; the soft blue impression on the paper is the source photograph's own shallow depth of field, not upscale mush. There is no vector overlay for softness to undermine (which is what made en ch1's s4 the stronger case), and it is a warm, well-composed closing frame | None. Spend the re-fetch budget on #1 and #5 instead |
| 8 | s2 | note | The alarm clock is upright, brightly lit and reading 12:00, where the script asked for *"an unlit bedside alarm clock face-down, nothing switched on"*. §10 already declares the relaxation | **Ruled: no action.** A photograph cannot show absence, only an object at rest — and that is this chapter's own established grammar: s1's face-down phone stands for *"the phone does not ring"* by exactly the same convention. Nothing in the frame says the alarm is set or ringing, and the negation is carried by the three chips, which is what the declared cascade is for | None |
| 9 | housekeeping | note | `renders/` still holds `DRAFT-ch1-v2/v3/v4.mp4` and `SHEET-ch1-v2/v3/v4.jpg` **plus a bare `SHEET.jpg`, all dated 08-07 — style A**. `SHEET.jpg` is the name a glob is most likely to pick up | The style-A sheet already almost got reviewed as current once this round (run.json records the 14-minute-stale sheet). A bare `SHEET.jpg` from the retired build sitting beside the real one is the same landmine with a friendlier filename | Delete or move the six style-A artefacts out of `renders/` before the next round |

## What is working

- **Every joint is clean, and I checked all seven from the encode, not just the one that was fixed.** At 4.45 / 8.60 / 12.45 / 18.21 / 21.88 / 30.05 / 35.40s exactly one headline is on screen at each mid-dissolve. The `#s4-stmt` fix holds and nothing else double-paints. The cue ladder is honest: kicker +0.30 (first cue well inside 0.5s), statement +1.10, foot +1.90, every gap 0.80s.
- **s6 is the best frame in the chapter and the model for #4.** Three drawn cells — bulb, sack, house — over three real jars, one drawn object per named cost, each taking its tick 0.35s after it lands, the whole row settled by +3.10. It is genuinely additive (the jars can say *ration* and cannot say *electricity* or *rent*), it closes a two-round blocker, and it earns its place under rule 8. Do not touch it.
- **s5's `275` plaque and s8's stamp both say their lines sound-off**, the ₹ banner lands the payoff at s4, and the chapter renders no figure at all so both rate asserts are correctly vacuous. Whatever the fix pass does, it must not disturb s5, s6 or s8.
