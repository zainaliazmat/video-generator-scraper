# editor · passive-income-number · hi · chapter 1 · attempt 2
VERDICT: REWORK

Reviewed from a freshly built `renders/SHEET.jpg` (7 cells, one per scene, from
`DRAFT-ch1-v3.mp4`) plus 10 sampled frames off the encoded mp4, the raw files in
`assets-ch1/final/` and their `.src` records, `index.html`, `assets/audio.json`,
`assets/cues-tables.json`, and the fixed `tools/audio/cues.py` re-run against this
chapter. Round-1 findings 2, 3, 6, 7 and 9 are **cleared** — see "Cleared" below.
One blocker remains, and it is the same slot as round 1.

## Findings

| # | scene | severity | what | why it fails | fix |
|---|-------|----------|------|--------------|-----|
| 1 | s5 | **blocker** | Three glass clip-top jars — rajma, green lentils, rice — on a wooden counter with a bamboo tray behind. `.src` query was `jars of rice and lentils on a wooden kitchen shelf warm light@pexels` | The line names **three** subjects — बिजली का बिल, राशन, किराया — and the frame carries **one**. Electricity absent, rent absent, and the line's *verb* (चुपचाप भरता रहता है — quietly keeps paying) absent entirely. Cover the words and this frame says "pantry", not "a corpus is covering your fixed costs". That is the sound-off rule's own failing condition, and the under-claim is not cosmetic: it shrinks the cold open's promise from *your fixed costs are covered* to *your groceries are covered*, which is not what the video is about. Note that round 2's query **abandoned** the bills rather than failing to find them — the storyboard's actual spec ("three household bills fanned on a table — electricity, a grocery slip, a rent receipt") has only been fetched against **once**, in round 1, with a query that asked for a paper stack | **Two routes, either clears it.** **A (try first, one attempt):** re-fetch on a query that actually asks for the bills — `electricity bill and rent receipt on a wooden table`, `utility bills and receipts close up`, `unpaid bills envelopes on a table`. **Warm, low-key original**, see #2 for the measured threshold. **B (fallback, and the stronger frame if A misses):** keep `s5.jpg` — it is a good warm domestic photograph and it legitimately carries *ration* — and amend §7's `art` column for scene 5 from `off` to a drawn layer: three marks (a bulb, a jar/sack, a house-or-key) taking a tick in sequence on the statement beat. `assets/icons/checkbox-tick.svg` already exists. Per `format.json vector_art.icon_first` this is inline `<svg class="icon">` + `draw()`, **not** a Lottie fetch — and the chapter has 3 of its 4 Lottie slots free if a real illustration turns out to be wanted |
| 2 | s5 (route A) | should-fix | Standing instruction on any re-fetch of this slot, carried from round-1 #4 and now **measurable** | I measured mean R−B on every ch1 raw: s5 **+61.7** and s7 **+43.6** render warm through the locked `grayscale(.32) brightness(.62)`; s2 **+14.6** and s3 **+3.3** do not and come out cool. A warm-*looking* raw is not enough — it has to be saturated enough to survive the grade | Reject any candidate under roughly **R−B +40** on a downsampled mean. Do **not** add a per-scene grade override; §9 forbids it |
| 3 | audio | should-fix | The repaired `cues.py` now emits **13** cues for ch1: the 6 joints + s3's `buzz` + a **`reveal` on all seven statement rises**. Storyboard §2's DRY BEATS list says *"Chapter 1 except s3's `buzz` … Scene joints still carry their `transition`; nothing else fires until 2.1"* — those seven reveals are precisely "something else firing". **13 is over-cued for this chapter.** What actually ships is `assets/audio.json`, hand-written by `build.mjs`, with the correct **7** | Not a ch1 defect *as built* — but `cues.py --write` writes straight into `assets/audio.json` (line 246) and would silently replace the correct list with the over-cued one. The generator has no notion of the declared DRY set, so it will do this on every chapter that has one: s41/s42/s45 (ch5), s63 (ch6), s69/s70 (ch6), plus the two priority overrides (s49 → `reveal` not `hero`, s24 → no content cue) | **Do not run `cues.py --write` on ch1.** Before ch5, add a `dry` key to `assets/cues-tables.json` — which already exists as the home for exactly this class of per-video fact — listing the declared-silent scenes, and have `cues.py` skip content cues (never joints) for them. `format.json`'s fix-defaults rule: this belongs in the table, not in a prose warning that the next run will not read |
| 4 | s2 | note | The frame renders cool/blue against §10's declared *"ch1 warm first light → **one cool frame at s3**"*, so s3 is no longer the chapter's single cool note | **Ruled: no action, and do not re-fetch this slot.** The round-1 blocker is fully cleared — the raw is a hot glass of chai steaming on a wooden windowsill, warm mullions, daylight bokeh through leaves, hard sun on the sill. The photograph is right and it is well composed. The coolness is a *system* outcome, not a photo defect: the raw measures R−B +14.6, warm but too desaturated to survive the locked grade, after which the cool field wins. §9 forbids a per-scene override and s2 carries no role colour so it carries no `--tint` either — there is no lever that does not break a rule. A fourth fetch to chase a temperature would risk losing a frame that finally says its line | — |
| 5 | s4 | note | The headline crosses the "275" plaque, the scene's one object | **Ruled: acceptable, ship it.** blockframe-9 *is* a centred stack over a full-bleed photograph — 71 of 78 scenes are `centred`, so type over subject is the architecture, not an accident. Measured Y 235 over a local ground of Y 41, crisp at 2×, and both the words and the digits stay readable through the overlap; the eye takes "a number" and a number in one fixation, which helps the line. I also checked the spoiler risk and it is not one: ch6's figures are ₹1,00,00,000 and ₹25,000/month, the plaque has no ₹ and no comma grouping, and it reads unmistakably as a scalloped door plate. The open loop is intact | Optional only, and not worth a fourth attempt on this slot: a crop that drops the plaque low-left |
| 6 | s7 | note | At the tightest end of the `i` push the counterweight arm sits in the outer ~6% and clips, so "two things together" leans on the small pale dish at lower left | The pan is loaded and the beam and chains are in frame throughout; sound-off it reads "weighing / measuring", which is the right neighbourhood for *every figure ships with its rate*. Round-1 #7 is cleared to an acceptable level | None required |

## What is working

- **s4 is the chapter's best frame now.** A white enamel "275" on a deep-red door under *"You just reached a number"* is a better idea than the storyboard's own brown envelope — it makes "reached" literal without naming a figure. Whatever the s5 fix does, do not touch s4 again.
- **The s3 Lottie is correct at last.** I read it out of the encode: exactly one ₹ in the app-mark square, two bars, no third, digits masked, amount unreadable, phone face-down, card complete before the 12.33 s dissolve. §8's open loop holds. Do not regenerate it.
- **s2 and s6 both now say their lines** — a window with tea and depth, and steps rising to a bright gap — and s1 and s7 still survive the grade. Five of seven cells are sound-off clean.
- **Seven distinct md5s, no repeat.** Spine exact: 1073 frames / 30 fps, `data-framings` matches each scene's own hold, s7 carries its bare 6.599 s, ladder at +0.30 / +1.10 / +1.90 with 0.80 s gaps and first cue ≤ 0.5 s. No rail, no chapter mark. None of the s5 fix touches any of this — it is an asset or an `art`-column change, not a re-time.

## Cleared since round 1
#2 (s2 had no window) · #3 (Lottie said nothing about money) · #5 (s3 glow) — s3 is dark but the Lottie now carries the beat, so this is spent · #6 (s6 buried the stair) · #7 (s7 pans empty and level) · #8 (`cues.py` parsed the wrong `<script>`; ch2 now scores 24) · #9 (`cues-tables.json` written, with holds/buzz/counted).

## One correction to my own round-1 review
I wrote that "ch1 does not want more [drawn art] — it has no number, comparison, process, date, share or count beat." That was wrong about **1.5**, which is a COUNT of three recurring obligations and a PROCESS (paid, quietly, every month) — the chapter's one legitimate drawn-art beat, and the reason route B above is the stronger of the two fixes rather than a consolation. It also means the storyboard's `art: off` on s5 is the file's **default** (74 of 78 scenes carry it), not a declared device like the SOLO or HOLD cases — so route B is a §7 amendment fin-storyboard owns, not a device being broken.
