---
summary: fin-editor en ch2 attempt 2 — PASS. Zero blockers. All three round-1 blockers verified fixed IN THE ENCODE, not on trust (s17 rings OCTOBER 1994 / FEBRUARY 1998; s14 splits dead centre; s20 has no person). The tone trade is ruled FAVOURABLE and I correct my own round-1 reading — the "arc" I approved was half an artefact of the blocker I was simultaneously calling out. Three should-fix left, all cheap, none false, none blocking. Explicit tone recommendation handed to the CEO, plus one cross-cut item both cuts have independently arrived at.
updated: 2026-08-08
source: renders/DRAFT-ch2.mp4 (13:41) + a freshly rebuilt SHEET.jpg read as a grid · 8 frames pulled from the encode · assets-ch2/final read for dupes by md5 · script-en.md ch2 lines 2.1-2.15 · storyboard-en.md · facts-staging.md · logs/editor-en-ch2-1.md · logs/fin-build-en-ch2-2.md · logs/fin-render-en-ch2-2.md · logs/editor-hi-ch2-styleE-1.md · tools/format.json vector_art
stage: fin-editor, cut en, chapter 2, attempt 2
---

# editor · passive-income-number · en · chapter 2 · attempt 2
VERDICT: PASS

Sheet rebuilt (`tools/chapter_sheet.py`, 16 cells) and read as a grid before anything
else, every cell placed beside its VO line. Eight frames pulled from the **encoded** mp4
for the calls the sheet could not settle: s17 @62.0, s14 @41.0, s20 @78.0/80.5/83.0 (plus
a 2× crop of the kicker), s13 @32.5, s15 @46.5, and the s20→s21 dissolve tail @84.69 at
+0.10 brightness / 2.0 contrast. Cue list re-derived independently from `assets/audio.json`.
`md5sum` across all 15 finals: **zero duplicates**, so no image is used twice.

## The three blockers — each verified on the frame, none taken on trust

**#1 s17 — FIXED.** At 62.0 the frame carries a time line with two ticks and
**OCTOBER 1994** and **FEBRUARY 1998**, each inside a drawn ellipse, under
`4.0% — a finding with a date. Not a promise.` Both dates are legible in the encode at
1080p. They are the chapter's own citations (s13's foot, s15's foot, and
`facts-staging.md` rows 50 and 53), so nothing is invented — `truth_bar` holds. Choosing
the script's own dates over `calendar-20th-circled.json` was the better call than the one
I suggested: a generic circled 20th would have put a number on screen that means nothing
here. The line **stopping** 120px past the second date is a real second sentence. The
stamp-box connotation I objected to is mitigated rather than removed, and I am not
pressing it — the drawn axis now owns the lower third and the type denies the promise.

**#2 s14 — FIXED.** One track, filled amber to the midpoint, grey beyond it, with an
opaque tick at the split. Measured by fin-render at **0.5006** and corroborated in source
by `SPLIT = 0.5` driving both the marker and the `span()` endpoint from one constant, so
picture and animation cannot disagree. Additive under rule 8: a card catalogue cannot say
*half, and half*. Leaving `.centred` and breaking the focal to two lines was the right
trade — the ladder floor was never approached and the focal still sits at the top step.
Accepting `.art-lift`'s rejection: a full-height p-c lift erasing the photograph is
exactly the defect rule 9 exists to prevent, and the A/B was done on the frame.

**#3 s20 — FIXED.** Read at 78.0, 80.5 and 83.0 plus a 2× crop. **No person, no torso, no
arm, no hand, no face.** A US produce shelf, handwritten `$ / LB` cards, `ITALIAN PEPPER`
/ `GREEN SQUASH` / `REGULAR EGGPLANT` — correct country, correct currency, no ₹ anywhere.
§10's hands-and-objects-only rule is no longer breached, and the precedent chapters 3–6
would have inherited is closed.

I also accept fin-assets' declared deviation from the storyboard's back-seat bags. Seven
sheets found that bags-in-a-car is a delivery-driver genre that always carries a person;
the subject moved and the line's *store* stayed. A produce shelf under
`PER MONTH / $847` says *this is the grocery bill* more plainly than two bags do. **Third
time this run a declared subject turned out not to exist as briefed** — that belongs in
the storyboard stage's feedback, not in ch2.

## THE RULING YOU ASKED FOR — the tone trade is FAVOURABLE. Take it. s20 stays.

And I am correcting my own round-1 reading, because it does not survive contact with the
new numbers.

**What I approved in round 1 was not an arc.** The segment curve was
50.0 → 49.5 → 50.2 → 47.9 → 44.7. The first three segments are flat within **0.7 of a
point** — that was already a plateau. The only descent was the last two steps, and the
first of those two, the 47.9, was pulled down by a **single scene at p90 35: s20**. The
same s20 I was calling a blocker in the same log. The 24-point span was 35→59, and the 35
end of it was the blocker.

So "a descending arc into the green close" was, to a material degree, one under-exposed
spec-violating photograph doing structural work I credited to design. Removing it did not
destroy a shape; it revealed that the shape was always **plateau-then-step**, and it moved
the one remaining step onto the boundary where it belongs.

**What the chapter is now:** a level amber body at ~50 for 84 seconds, then a single
6-point drop into the green corpus close at 44.7. That step now coincides with the colour
turn (`--target` amber → `--fund` green), the ground turn (`#2a2113` → `#12351f`) and the
semantic turn (evidence → payoff). One tonal event, landing on the chapter's one
structural event. The old curve had a spurious dip in the *middle* of the green method run
and then a second dip — two events, one of which meant nothing.

**Losing the 35 floor is worth it, and it cost nothing at the hero.** The predicted
regression did not appear in either direction: s19→s20 improved (−23 → −17) and s20→s21
went **flatter**, +7 → +0, not steeper. That both magnitude predictions were wrong is a
real lesson for the fetch stage — the encoded p90 is not a linear function of source p90,
the ratio column in fin-render §4c falls monotonically as the source brightens, and any
future "this fetch will land at X encoded" should carry that compression — but it is a
lesson about prediction, not a reason to move a frame.

**No frame moves.** s9 is unchanged and does not need to change for this chapter to meet
its requirement (see the CEO section). The one optional improvement is s10's photograph
and I am handing it up as a choice, not a finding.

## Findings

| # | scene | severity | what | why it fails | fix |
|---|-------|----------|------|--------------|-----|
| 1 | s15 | should-fix | A closed white book macro that the ken push turns fully abstract. At 46.5 in the encode it is an unidentifiable pale curve — you cannot tell it is a book, a page, or paper. The line is *"three Trinity University professors ran it again and **published** it"* | Sound-off: cover the words and this frame says nothing at all. It is the one photograph in the chapter that fails on its own terms. **I am NOT blocking on it** and I want the reason on the record: I graded it should-fix in round 1 on unchanged evidence, so escalating it now would be moving the goalposts on a pass that fixed everything I asked for; the beat's content is a name and a date and **both are on screen** in the focal and the foot; and it is bland, not false — it does not contradict its line. Also confirmed **not** a twin of s13 as I implied in round 1: s13 at 32.5 is unmistakably an open journal spread, so the "same idea twice" half of that finding was overstated | Re-fetch to the storyboard's own cue: **a bound journal volume open flat, spine cracked, on a library table, warm lamp, no legible title**. Beside s13's open page it differs by scale and context, and it restores the *published* reading. **Needs a new fetch — do not reuse s13.jpg or any file in this chapter or ch1.** Ride it with the next asset pass rather than spending a ch2 round on it |
| 2 | s20 | should-fix | `PER MONTH` sits on the ITALIAN EGGPLANT price card, and the card's own orange `$5.` ink crosses the kicker's baseline through `R M O`. Confirmed at 2× at 78.0 and 83.0; the ken is a slow push so the card stays behind the kicker for the whole 7.5s | A type collision, not a contrast failure — fin-render's 2.67 is the chapter's lowest of eight kickers but still inside the band (median 2.95). What it actually costs is tidiness on the frame immediately before the hero, and there is a `$` in blurred orange sitting one line above `$847`. The three other legible dollar amounts do **not** compete (Weber 0.28–1.55 vs the focal's 21.57) and they read as *grocery prices*, which is the scene's subject — leave those alone | Build-only, no fetch, no asset change: either offset the `ken` start x so the card sits left of the stack, or put a **`.band` behind the stack**, which this chapter already uses at s17 and which is the prescribed mechanism. Cheapest of the three |
| 3 | s14 | should-fix | The unfilled grey half reads **81.0** luma against the amber filled half's **59.3** over a 35.6 ground — the empty side is 21.7 brighter than the filled side, on the frame whose job is to say 50/50 | Not false and not a proportion error: the split geometry is exact and a viewer reads bar **length**, not luma, for proportion, so the frame does not argue with its line. But the chapter's `--target` emphasis colour is the quieter half, and at a glance the bonds side dominates a bar that exists to say the two are equal | One-line opacity change: raise the amber fill or drop the grey track below its current `.22` until the two halves read at comparable weight. Do not touch the split constant, the tick, or the `art-forward` `.52` |

## Rulings — settled, do not reopen

- **The `$847` ghost at s20→s21 is not a defect.** I pulled the frame at +0.380 and pushed
  it +0.10 brightness / 2.0 contrast: `$847` is a smudge and `RUNG ONE` is arriving. At
  native gain it is invisible. 3.1% is the tail of a correct dissolve, visible only because
  s21's ground is the chapter's darkest. This is **not** the japanese-money-methods
  signature (that is outgoing text at ~100% for the full 0.45s); all 14 joints sit at
  1.2–3.5% by +0.380 and `isolation: isolate` is present.
- **The s18 cue gap is fixed and I verified it myself** rather than reading the claim:
  re-derived from `assets/audio.json`, **25 cues, first at 0.000, minimum gap 1.100s**, and
  the s18 pair is now 62.658 → 63.758. `cues.py` exit 0.
- **Drawn-art density is AT the cap, not over it.** Four layers against
  `vector_art.lottie.max_per_chapter: 4`. All four pass rule 8's additive test — the 95/100
  grid, the ÷4% = ×25 block, the 0.500 split, and the two ringed dates each assert
  something the photograph underneath structurally cannot. All four check out against
  `facts-staging.md` (Trinity Table 3 95% at 50/50 and at 100% stocks; Bengen Oct 1994;
  Cooley/Hubbard/Walz Feb 1998; BLS $10,169). **Chapters 3–6 must not read four as the new
  baseline** — §8's budgets (ch3 1, ch4 1, ch5 0, ch6 3) stand, and any fifth layer here
  would have to displace one of these.
- **s19 downgraded from should-fix to note.** My round-1 objection was that an egg carton
  and two carrots under-argue a $10,169 annual total. That still holds, but s20 is now
  unambiguously a *store*, which makes s19 the differentiated *home table* half of the pair
  and does real work the two-produce-frames-in-a-row reading did not have. Not worth a
  fetch on its own; fold it in only if s15 is being re-fetched anyway.
- Carried unchanged from round 1 and still true: the rate constraint is met on screen on
  all four figure frames; the tank/tap mitigation lands; s22's `tick` is KEEP; the four
  joints pass as cuts; s23 is not a reuse of ch1's s8.
- Craft, re-checked clean: 15/15 `data-framings` present and each equal to
  `duration − 0.45` (s10 splits 6.81 + 3.786), **s23 carries its bare 7.749**, first cue at
  0.000 against `first_cue_by_seconds` 0.5, centred focals within 1.0px of 960, s21's track
  mid 959.5, no type collisions at any sampled frame other than finding #2.

## TONE — my explicit recommendation for the CEO gate

**Recommendation: ACCEPT en ch2's tone as it stands. Do not spend another round on it.**

**Against ch1, the requirement is met on both readings, measured.** The CEO required ch2
to open measurably lighter than ch1. It does: **s9 at p90 57 against ch1's 45, +12**. And
the whole chapter now weighs **49.2 duration-weighted against ch1's 36–48 band** — ch2 sits
*above* ch1's ceiling, not at it, which is the stronger of the two readings and was not
true last round (48.4, i.e. exactly at the ceiling). Asking for more than that is taste,
and it costs a fetch plus a rebuild plus a re-draft on a chapter that is otherwise clean.

**The shape to describe to the creator** is not "an arc". It is *a level amber evidence
body, then one step down into the green payoff*. That is defensible and I would defend it:
one tonal event, landing on the one place the chapter changes what it is doing.

**If the CEO wants the lighter open FELT rather than merely measured, there is exactly one
lever and it is s10's photograph.** s10 is 10.6s — 10% of the chapter — at p90 44, so the
chapter's most-watched stretch after the hook is its second-darkest. The fix is a
daylight-side exposure of the same brass-tap material family, and possibly just a raking
crop of the existing Pexels source. **It is not `.scrim`**, and s20 has now proved that
empirically: swapping one photograph moved that scene's p90 by **+8** and its p10 by
**0** (15 → 15). The photograph owns the highlights; the scrim's four stacked gradients own
the shadows, and raising the floor greys every still in the chapter.

**⚠ The cross-cut item, and it is the one thing I want the CEO to actually decide:**
**both cuts have independently landed their hero corpus in their darkest frames.** en's
s21 `$254,225` sits at p90 **44** against a chapter range of 43–59 — the payoff is in the
chapter's floor. hi ch2 reported the same inversion from the other side and harder: its
hero s16 is the chapter's **trough at 37.3** while a blank notebook page holds an 11.6s
plateau at **59.0** — light landing on the least eventful picture and draining off the most
important one.

The difference is provenance, and it matters. In **en** it is caused by a *declared* device
— the `--fund` green ground `#12351f`, role-deepening approved through ch1 — so en's
version is a consequence of a decision someone made. In **hi** it is undeclared drift. But
the audience cannot tell the difference, and two cuts arriving at the same inversion by
different routes is the signal that the style itself, not either chapter, is what produced
it. **If the payoff should be the brightest thing on screen, that is a change to style E's
green ground and it has to be set ONCE, across both cuts, before ch3 builds** — it is not a
ch2 edit and I am not asking for one. If the green run is meant to be the quiet, resolved
end of a chapter, then en ch2 is already correct and hi ch2 is the one that needs work.

## What is working

- **All three blockers were fixed by the cheapest honest route available** — two by drawing
  rather than fetching, one by a single in-place re-fetch — with no timing, no framing, no
  rate-assert and no cue-count change. The chapter is byte-for-byte the same 105.518s /
  15 scenes it was, and every fix is visible in the encode. This is what a targeted fix
  pass should look like.
- **The rate discipline is intact and untouched.** Four figure frames, four assumptions on
  screen, `AT A 4.0% WITHDRAWAL RATE` at 40px above the hero and `ILLUSTRATIVE ARITHMETIC`
  in its foot. Do not weaken any of those four foots to make room for anything.
- **The four drawn layers are the standard chapters 3–6 should be held to.** Every one of
  them states a measurement the photograph beneath it cannot, every one is computed from a
  real figure, none is depictive, and all four finish well before their cut.
- **The dead local CSS came out and the geometry survived it** — centred focals within
  1.0px of 960 and s21's track mid at 959.5 with the patch gone. That is the actual proof
  the upstream port replaced the patch rather than duplicating it, and chapters 3–6 will
  not now inherit a "SYSTEM GAP" comment describing a gap that no longer exists.
