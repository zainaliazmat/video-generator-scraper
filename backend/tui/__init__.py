"""ytauto — the terminal UI front end for the YouTube scraper.

This package is the only front end. It drives the scraper, the AI prediction,
and the history diff by calling backend functions in-process (no HTTP, no
browser). Launch it with `python -m tui`, `./run.sh`, or the `ytauto` command.
"""
