yt-dlp>=2025.1.1
openpyxl>=3.1.0
claude-agent-sdk>=0.2.0   # only used by ideas.py (optional AI content plan; runs
                          # through your Claude subscription via the Claude Code CLI)

# Web app (server/) — local FastAPI backend that drives the scraper
fastapi>=0.110
uvicorn>=0.29
sse-starlette>=2.0
python-multipart>=0.0.9   # required by FastAPI for multipart/form-data (file upload)

# Test-only
pytest>=8.0
httpx>=0.27
