yt-dlp>=2025.1.1
claude-agent-sdk>=0.2.0   # used by ideas.py for the AI prediction; runs through
                          # your Claude subscription via the Claude Code CLI

# Terminal UI (tui/) — the front end; drives the scraper in-process
textual>=0.60

# Test-only
pytest>=8.0
pytest-asyncio>=0.23      # for the headless Textual (Pilot) smoke tests
